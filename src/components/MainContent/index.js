import React from "react";

function Hero({ data, title }) {
  const { subline } = data || {};

  return (
    <div className="">
      <div className="container block-padding">main content</div>
    </div>
  );
}

export default Hero;
